
namespace Barebone.Game.Input
{
    /// <summary>
    /// Based on SDL3 so we can cast instead of map.
    /// </summary>
    public enum MouseButton
    {
        Left = 1,
        Middle = 2,
        Right = 3,
        X1 = 4,
        X2 = 5,
    }
}
